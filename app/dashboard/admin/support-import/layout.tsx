/** Support import of multi-year Acuity diaries needs a long serverless window. */
export const maxDuration = 300;

export default function SupportImportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
